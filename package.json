{
  "name": "pendo-guide",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-sass": "node-sass scss/mri-ui.scss css/mri-ui.css",
    "watch-sass": "node-sass -w \"scss/**/*.scss\" -o \"css/mri-ui.css\" --output-style compressed",
    "start": "browser-sync start --server --files \"*.html\" \"*.css\" --no-open --no-notify"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.26.13"
  },
  "devDependencies": {
    "node-sass": "^5.0.0",
    "nodemon": "^2.0.7"
  }
}
